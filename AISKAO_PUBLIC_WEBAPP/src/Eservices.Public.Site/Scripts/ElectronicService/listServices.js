
//$(document).ready(function () {

    //Services search

    //$("#buttonSearch").click(function (event) {
    //    var currFilterText = $("#filterServices").val();
    //    if (currFilterText) {
    //        var rowCount = $('#servicesList tbody tr').length;
    //        var count = 0;
    //        //filter
    //        $("#servicesList tbody tr").each(function () {
    //            var serviceNameCell = $(this).children(".serviceNameCell")[0];

    //            var currSeriveName = $(serviceNameCell).text();
    //            if (currSeriveName.toLowerCase().indexOf(currFilterText.toLowerCase()) == -1) {
    //                $(this).addClass("noMatch");
    //                count = ++count;

    //            } else {
    //                $(this).removeClass("noMatch");
    //                $("#servicesList").show();
    //                $("#message").hide();
    //            }
    //            if (count == rowCount) {
    //                $("#servicesList").hide();
    //                $("#message").show();
    //            }
    //        });
    //    } else {
    //        $("#servicesList tr.noMatch").each(function () {
    //            $(this).removeClass("noMatch");
    //            $("#servicesList").show();
    //            $("#message").hide();
    //        });

    //    }

    //});
//});